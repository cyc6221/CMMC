---
layout: page
title: "[BD20] 4 Schnorr Identification and Signatures from MBDL"
author1: "Mihir Bellare"
author2: "Wei Dai"
venue: "INDOCRYPT 2020"
date: 2026-04-09
last_updated: 2026-04-15
label: "paper"
tags: [square-root-barrier, paper]
---

It is chapter 4 of the paper [The Multi-Base Discrete Logarithm Problem: Tight Reductions and Non-Rewinding Proofs for Schnorr Identification and Signatures]({{ "/articles/paper/BD20/" | relative_url }}). Other chapters include:

- [1 Introduction]({{ "/articles/paper/BD20/BD20-1/" | relative_url }})
- [2 Preliminaries]({{ "/articles/paper/BD20/BD20-2/" | relative_url }})
- [3 The Multi-Base Discrete Logarithm Problem]({{ "/articles/paper/BD20/BD20-3/" | relative_url }})
- [5 MBDL hardness in the Generic Group Model]({{ "/articles/paper/BD20/BD20-5/" | relative_url }})
- [A Okamoto Identification and Signature from MBDL]({{ "/articles/paper/BD20/BD20-A/" | relative_url }})
- [B Ratio-based tightness]({{ "/articles/paper/BD20/BD20-B/" | relative_url }})

---

## Identification Schemes and the IMP-PA Game

See [4.1 Schnorr Identification from MBDL]({{ "/articles/paper/BD20/BD20-4.1/" | relative_url }}).

## Tight Reduction from Schnorr Identification to 1-MBDL

<div class="theorem">
<strong>Theorem 4.1</strong>
Let $\mathbb{G}$ be a group of prime order $p = \lvert \mathbb{G} \rvert$ and $g \in \mathbb{G}^\ast$ be a generator of $\mathbb{G}$.
<br>
Let $\mathrm{ID} = \mathrm{SchID}[\mathbb{G}, g]$ be the Schnorr identification scheme.
<br>
Let $\mathcal{A}$ be an adversary attacking the imp-pa security of $\mathrm{ID}$.
<br>
Then we can construct an adversary $\mathcal{B}$ such that

$$
\mathbf{Adv}^{\mathrm{imp\text{-}pa}}_{\mathrm{ID}}(\mathcal{A})
\le
\mathbf{Adv}^{\mathrm{mbdl}}_{\mathbb{G},g,1}(\mathcal{B}) + \frac{1}{p}.
$$

Additionally, $T_{\mathcal{B}}$ is roughly $T_{\mathcal{A}}$ plus simulation overhead
$\mathcal{O} (Q^{Tr}_{\mathcal{A}} \cdot T^{\mathrm{exp}}_\mathbb{G})$.</div>

<div class="algorithm">
  <div class="algorithm-title">Adversary $\mathcal{B}^{\mathrm{DLO}}$</div>

  <p><strong>Init:</strong></p>
  <ol>
    <li>
      <code>Set</code>
      $(Y, X) \xleftarrow{\$} \mathrm{Init}()$
    </li>
    <li>
      <code>Run</code>
      $z^\ast \xleftarrow{\$} A^{\mathrm{Ch}, \mathrm{Tr}}(X)$
    </li>
    <li>
      <code>Return</code>
      $z^\ast$
    </li>
  </ol>

  <p><strong>Ch($R^\ast$):</strong></p>
  <ol>
    <li>
      <code>Set</code>
      $W \leftarrow R_{\ast}^{-1} \cdot Y$
    </li>
    <li>
      <code>Set</code>
      $c^\ast \leftarrow \mathrm{DLO}(1, W)$
    </li>
    <li>
      <code>Return</code>
      $c^\ast$
    </li>
  </ol>

  <p><strong>Tr:</strong></p>
  <ol>
    <li>
      <code>Set</code>
      $z \xleftarrow{\$} \mathbb{Z}_p$
    </li>
    <li>
      <code>Set</code>
      $c \xleftarrow{\$} \mathbb{Z}_p$
    </li>
    <li>
      <code>Set</code>
      $R \leftarrow g^z \cdot X^{-c}$
    </li>
    <li>
      <code>Return</code>
      $(R, c, z)$
    </li>
  </ol>
</div>

### Quantitative Comparison

## Signature Schemes and the Transfer from IMP-PA to UF

See [4.2 Schnorr Signature from MBDL]({{ "/articles/paper/BD20/BD20-4.2/" | relative_url }}).

<div class="lemma">
<strong>Lemma 4.2 [1]</strong>
Let $\mathbb{G}$ be a group of prime order $p = \lvert \mathbb{G} \rvert$ and $g \in \mathbb{G}^\ast$ be a generator of $\mathbb{G}$.
<br>
Let $\mathrm{ID} = \mathrm{SchID}[\mathbb{G}, g]$ and $\mathrm{DS} = \mathrm{SchID}[\mathbb{G}, g]$ be the Schnorr identification and signature schemes, respectively.
<br>
Let $\mathcal{A}_{\mathrm{ds}}$ be an adversary attacking the uf-security of $\mathrm{DS}$. Let

$$
\alpha =
(1 + Q^{\mathrm{H}}_{\mathcal{A}_{\mathrm{ds}}} + Q^{\mathrm{SIGN}}_{\mathcal{A}_{\mathrm{ds}}})
\cdot
Q^{\mathrm{SIGN}}_{\mathcal{A}_{\mathrm{ds}}} .
$$

Then we can construct an adversary $\mathcal{A}_{\mathrm{id}}$ such that

$$
\mathbf{Adv}^{\mathrm{uf}}_{DS}(\mathcal{A}_{\mathrm{ds}})
\le
(1 + Q^{\mathrm{H}}_{\mathcal{A}_{\mathrm{ds}}})
\cdot
\mathbf{Adv}^{\mathrm{imp\text{-}pa}}_{\mathrm{ID}}(\mathcal{A}_{\mathrm{id}})
+
\frac{\alpha}{p}.
$$

Additionally,
$T_{\mathcal{A}_{\mathrm{id}}} \approx T_{\mathcal{A}_{\mathrm{ds}}}$
and
$Q^{\mathrm{Tr}}_{\mathcal{A}_{\mathrm{id}}} = Q^{\mathrm{SIGN}}_{\mathcal{A}_{\mathrm{ds}}}$.
</div>

## Improved Bound for Schnorr Signatures from 1-MBDL

將 Theorem 4.1 與 Lemma 4.2 結合後，可直接得到 Schnorr signatures 的新界。

<div class="theorem">
<strong>Theorem 4.3</strong>
Let $G$ be a group of prime order $p = \lvert G \rvert$, and let $g \in G^\ast$ be a generator of $G$. Let $DS = \mathrm{SchSig}[G, g]$ be the Schnorr signature scheme. Let $A$ be an adversary attacking the uf security of $ID$. Let
$$
\beta = (1 + QH_A + QSign_A)QSign_A + (1 + QH_A).
$$
Then we can construct an adversary $B$ such that
$$
Adv^{\mathrm{uf}}_{DS}(A) \le (1 + QH_A) \cdot Adv^{\mathrm{mbdl}}_{G,g,1}(B) + \frac{\beta}{p}. \tag{13}
$$
Additionally, $T_B$ is roughly $T_A$ plus simulation overhead $O(QSign_A \cdot T^{\mathrm{exp}}_G)$.</div>

### Comparison with prior bounds

若將 Lemma 4.2 與前述 DL-based identification bound 結合，只能得到
$$
Adv^{\mathrm{uf}}_{DS}(A)
\le
(1 + QH_A) \cdot \sqrt{Adv^{\mathrm{dl}}_{G,g}(B)} + \frac{\beta}{p}. \tag{14}
$$

更好的既有分析則是直接應用 general Forking Lemma，得到
$$
Adv^{\mathrm{uf}}_{DS}(A)
\le
\sqrt{(1 + QH_A)\cdot Adv^{\mathrm{dl}}_{G,g}(B)} + \frac{\beta}{p}. \tag{15}
$$

式 (15) 優於式 (14)，因為 $1 + QH_A$ 被放進了平方根中。  
不過，式 (13) 進一步避免了平方根型態的鬆弛，因此在 quantitative bound 上更強。
