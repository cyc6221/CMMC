---
layout: page
title: "[BD20] 4 Schnorr Identification and Signatures from MBDL"
author1: "Mihir Bellare"
author2: "Wei Dai"
venue: "INDOCRYPT 2020"
date: 2026-04-09
last_updated: 2026-04-15
label: "paper"
tags: [square-root-barrier, paper]
---

It is chapter 4 of the paper [The Multi-Base Discrete Logarithm Problem: Tight Reductions and Non-Rewinding Proofs for Schnorr Identification and Signatures]({{ "/articles/paper/BD20/" | relative_url }}). Other chapters include:

- [1 Introduction]({{ "/articles/paper/BD20/BD20-1/" | relative_url }})
- [2 Preliminaries]({{ "/articles/paper/BD20/BD20-2/" | relative_url }})
- [3 The Multi-Base Discrete Logarithm Problem]({{ "/articles/paper/BD20/BD20-3/" | relative_url }})
- [5 MBDL hardness in the Generic Group Model]({{ "/articles/paper/BD20/BD20-5/" | relative_url }})
- [A Okamoto Identification and Signature from MBDL]({{ "/articles/paper/BD20/BD20-A/" | relative_url }})
- [B Ratio-based tightness]({{ "/articles/paper/BD20/BD20-B/" | relative_url }})

---

## Identification Schemes and the IMP-PA Game

### Canonical identification scheme

先固定 canonical identification scheme 的一般形式。  
一個 canonical identification scheme 由五個演算法
$ID = (\mathrm{Kg}, \mathrm{Cmt}, \mathrm{Rsp}, \mathrm{Vf}, \mathrm{Ch})$
組成。

$\mathrm{Kg}$ 是 key-generation algorithm，輸入安全參數後輸出 verification key 與 matching secret key。  
$\mathrm{Cmt}$ 是 commitment-generation algorithm，輸入 verification key 後輸出 commitment $R$ 與 state $st$。  
$\mathrm{Rsp}$ 是 response-generation algorithm，輸入 secret key、challenge 與 state 後輸出 response。  
$\mathrm{Vf}$ 是 deterministic verification algorithm，輸入 verification key、commitment、challenge 與 response，輸出 accept 或 reject。  
$\mathrm{Ch}$ 指定 challenge space。  

correctness 要求是：若 $(vk, sk)$ 由 $\mathrm{Kg}$ 正確產生，$R, st$ 由 $\mathrm{Cmt}(vk)$ 產生，$c$ 均勻取自 $\mathrm{Ch}$，且 $z \gets \mathrm{Rsp}(sk, c, st)$，則 $\mathrm{Vf}(vk, R, c, z)$ 應接受。

### IMP-PA game

對 Schnorr identification 考慮的安全性目標是 impersonation under passive attack。  
在這個遊戲中，對手先取得由 transcript oracle 提供的 honest transcripts，之後進行一次 impersonation attempt：先輸出一個 commitment $R^\*$，接著收到 challenge $c^\*$，最後輸出 response $z^\*$。若 $\mathrm{Vf}(vk, R^\*, c^\*, z^\*)$ 接受，則對手獲勝。

其 advantage 記為
$$
Adv^{\mathrm{imp\text{-}pa}}_{ID}(A)
=
\Pr\big[ G^{\mathrm{imp\text{-}pa}}_{ID}(A) \Rightarrow 1 \big].
$$

### Schnorr identification scheme

令 $G$ 為一個 prime-order group，$p = \lvert G \rvert$，且 $g \in G^\ast$ 為 generator。  
Schnorr identification scheme 記為
$ID = \mathrm{SchID}[G, g]$。

secret key 為 $x \gets \mathbb{Z}_p$，public key 為
$$
X = g^x .
$$

在一次 honest execution 中，prover 取 $r \gets \mathbb{Z}_p$，送出
$$
R = g^r .
$$
verifier 取 challenge
$$
c \gets \mathbb{Z}_p .
$$
prover 回覆
$$
z = r + cx \bmod p .
$$
verifier 接受當且僅當
$$
g^z = R X^c .
$$

### Prior result

對於 Schnorr identification，既有的 DL-based 證明給出以下界：
$$
Adv^{\mathrm{imp\text{-}pa}}_{ID}(A)
\le
\sqrt{Adv^{\mathrm{dl}}_{G,g}(B)} + \frac{1}{p}. \tag{7}
$$

這個界反映了經典的 square-root loss。  
在這裡，proof 的核心是 rewinding reduction。

## Tight Reduction from Schnorr Identification to 1-MBDL

以 1-MBDL 作為底層問題，並給出一個 tight reduction。

<div class="theorem">
<strong>Theorem 4.1</strong>
Let $G$ be a group of prime order $p = \lvert G \rvert$, and let $g \in G^\ast$ be a generator of $G$. Let $ID = \mathrm{SchID}[G, g]$ be the Schnorr identification scheme. Let $A$ be an adversary attacking the imp-pa security of $ID$. Then we can construct an adversary $B$ (shown explicitly in Figure 5) such that
$$
Adv^{\mathrm{imp\text{-}pa}}_{ID}(A) \le Adv^{\mathrm{mbdl}}_{G,g,1}(B) + \frac{1}{p}. \tag{8}
$$
Additionally, $T_B$ is roughly $T_A$ plus simulation overhead $O(QTr_A \cdot T^{\mathrm{exp}}_G)$.</div>

### Reduction approach

既有 DL-based reduction 會把 challenge point $Y$ 植入為 public key $X$，並藉由兩次相關執行取得兩份 compatible transcripts，再利用 special soundness 抽出 secret key。  
這個方法依賴 rewinding，因此導致 square-root loss。

這裡採用不同的做法。  
給定 1-MBDL instance，其中 challenge 為 $Y$，並可對某個 base $X_1$ 使用一次 discrete-log oracle。  
reduction 不把 $Y$ 設成 public key；相反地，將 $X_1$ 設成 public key，然後只執行 adversary 一次，並把 $Y$ 植入 communication 中，使得 adversary 在 impersonation 階段若成功，就會產生一個 response，可直接還原出 $DL_{G,g}(Y)$。

### Distribution issue

這個構造需要額外處理分布上的細節。  
MBDL instance 中給的是 generator $X_1$，而 Schnorr public key 在正常 key generation 下則是由均勻的 secret exponent 所產生。  
此外，challenge 的產生方式也需要確認與 IMP-PA game 所要求的分布一致。  
這些部分在後續 proof 中以 games 方式處理；此處先保留結果與整體方向，不展開證明。

## Signature Schemes and the Transfer from IMP-PA to UF

### Signature schemes

接著固定 signature scheme 的一般形式。  
一個 signature scheme
$DS = (\mathrm{Kg}, \mathrm{Sign}, \mathrm{Vf}, \mathrm{HF})$
包含 key-generation、signing、verification 以及 hash-function space。

在 UF game 中，會先均勻選取
$$
h \gets DS.\mathrm{HF},
$$
然後 adversary 可查詢 signing oracle，最後輸出一個 message-signature pair。若該 pair 驗證通過，且該 message 未曾向 signing oracle 查詢過，則 forgery 成功。

### Schnorr signatures

Schnorr signature scheme 記為
$$
DS = \mathrm{SchSig}[G, g].
$$
它是由 Schnorr identification scheme 經 Fiat--Shamir transform 得到。  
此時 hash function space 是所有從
$G \times \{0,1\}^\ast$
到
$\mathbb{Z}_p$
的函數。

### From UF to IMP-PA

在將 1-MBDL 的結果延伸到簽章之前，先使用既有引理，將 UF-security 的問題轉成 identification 的 IMP-PA 問題。  
這一步不改變章節主線，而是提供中介橋樑，使上一節對 Schnorr identification 的 tight reduction 能直接被用在 Schnorr signatures 上。

<div class="lemma">
<strong>Lemma 4.2 [1]</strong>
Let $G$ be a group of prime order $p = \lvert G \rvert$, and let $g \in G^\ast$ be a generator of $G$. Let $ID = \mathrm{SchID}[G, g]$ and $DS = \mathrm{SchID}[G, g]$ be the Schnorr identification and signature schemes, respectively. Let $A_{ds}$ be an adversary attacking the uf-security of $DS$. Let
$$
\alpha = (1 + QH_{A_{ds}} + QSign_{A_{ds}})QSign_{A_{ds}} .
$$
Then we can construct an adversary $A_{id}$ such that
$$
Adv^{\mathrm{uf}}_{DS}(A_{ds}) \le (1 + QH_{A_{ds}}) \cdot Adv^{\mathrm{imp\text{-}pa}}_{ID}(A_{id}) + \frac{\alpha}{p}.
$$
Additionally, $T_{A_{id}} \approx T_{A_{ds}}$ and $QTr_{A_{id}} = QSign_{A_{ds}}$.</div>

## Improved Bound for Schnorr Signatures from 1-MBDL

將前一節的 Lemma 4.2 與 Theorem 4.1 結合後，可直接得到 Schnorr signatures 的新界。

<div class="theorem">
<strong>Theorem 4.3</strong>
Let $G$ be a group of prime order $p = \lvert G \rvert$, and let $g \in G^\ast$ be a generator of $G$. Let $DS = \mathrm{SchSig}[G, g]$ be the Schnorr signature scheme. Let $A$ be an adversary attacking the uf security of $ID$. Let
$$
\beta = (1 + QH_A + QSign_A)QSign_A + (1 + QH_A).
$$
Then we can construct an adversary $B$ such that
$$
Adv^{\mathrm{uf}}_{DS}(A) \le (1 + QH_A) \cdot Adv^{\mathrm{mbdl}}_{G,g,1}(B) + \frac{\beta}{p}. \tag{13}
$$
Additionally, $T_B$ is roughly $T_A$ plus simulation overhead $O(QSign_A \cdot T^{\mathrm{exp}}_G)$.</div>

### Comparison with prior bounds

若將 Lemma 4.2 與前述 DL-based identification bound 結合，只能得到
$$
Adv^{\mathrm{uf}}_{DS}(A)
\le
(1 + QH_A) \cdot \sqrt{Adv^{\mathrm{dl}}_{G,g}(B)} + \frac{\beta}{p}. \tag{14}
$$

更好的既有分析則是直接應用 general Forking Lemma，得到
$$
Adv^{\mathrm{uf}}_{DS}(A)
\le
\sqrt{(1 + QH_A)\cdot Adv^{\mathrm{dl}}_{G,g}(B)} + \frac{\beta}{p}. \tag{15}
$$

式 (15) 優於式 (14)，因為 $1 + QH_A$ 被放進了平方根中。  
不過，式 (13) 進一步避免了平方根型態的鬆弛，因此在 quantitative bound 上更強。
